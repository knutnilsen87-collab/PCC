{
  "name": "@pcc/mobile-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo dev:mobile-web",
    "build": "echo build:mobile-web",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo test:mobile-web",
    "lint": "echo lint:mobile-web"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
